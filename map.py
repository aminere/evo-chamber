
import pygame as pg

class Map:
    def __init__(self, size):
        worldSize = WORLD_WIDTH * tileW, (WORLD_HEIGHT + 1) * tileH / 2
        self.tiles = pg.Surface(worldSize)

    def draw():
        print("draw")